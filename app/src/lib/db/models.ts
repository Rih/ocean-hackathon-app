export const user = () => {
    return `
        ++_id,
        id,
        nombre,
        estado
    `;
}

export const evaluation = () => {
    return `
        ++_id,
        beachId,
        apreciationCode
    `;
}